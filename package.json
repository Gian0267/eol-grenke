{
  "name": "nsm-eol-grenke-template",
  "version": "0.1.0",
  "private": true,
  "description": "Template applicativo per la gestione del fine noleggio dei contratti FLEX Grenke — Smartcom Solutions / Noleggio Su Misura",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev --workspace=backend\" \"npm run dev --workspace=frontend\"",
    "dev:backend": "npm run dev --workspace=backend",
    "dev:frontend": "npm run dev --workspace=frontend",
    "build": "npm run build --workspace=backend && npm run build --workspace=frontend",
    "db:setup": "npm run db:setup --workspace=backend",
    "db:seed": "npm run db:seed --workspace=backend",
    "db:reset": "npm run db:reset --workspace=backend",
    "db:studio": "npm run db:studio --workspace=backend",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "demo:reset": "npm run demo:reset --workspace=backend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "prisma": "^6.19.3"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
